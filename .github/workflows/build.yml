name: CoreMiner pipeline
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkouti repository and submodules
      uses: actions/checkout@v2
      with:
        submodules: recursive
        token: ${{secrets.SECRET_TOKEN}}
    - name: Bump version
      id: version_bump
      #if: github.ref == 'refs/heads/master'
      uses: anothrNick/github-tag-action@1.13.0
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        DEFAULT_BUMP: "patch"
        RELEASE_BRANCHES: "master"
        WITH_V: true
    - name: docker login
      run: docker login ghcr.io -u ${{ github.actor }} -p ${{ secrets.SECRET_TOKEN }}
    - name: docker build
      run: docker build . -t ghcr.io/core-coin/go-core/gocore:${{steps.version_bump.outputs.tag}} -t ghcr.io/core-coin/go-core/gocore:latest
    - name: docker push
      run: docker push --all-tags ghcr.io/core-coin/go-core/gocore
